{{ printf "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" | safeHTML }}
<urlset xmlns="https://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xsi="https://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="https://www.sitemaps.org/schemas/sitemap/0.9 https://www.sitemaps.org/schemas/sitemap/0.9/sitemap.xsd" xmlns:xhtml="http://www.w3.org/1999/xhtml" xmlns:image="http://www.google.com/schemas/sitemap-image/1.1">
  {{- /* {{- range .Data.Pages }} */ -}}
  {{- range or (where .Data.Pages "Type" "in" site.Params.mainTypes) }}
    {{ if or (.Params.relcanonical) (.Permalink) }}
      {{- $canonurl := urls.Parse .Params.relcanonical -}}
      {{- $escapedurl := printf "%s://%s%s" $canonurl.Scheme $canonurl.Host $canonurl.EscapedPath -}}
    <url>
      <loc>{{ or ($escapedurl) (.Permalink) }}</loc>
      {{ if not .Lastmod.IsZero }}<lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05Z" ) }}</lastmod>{{ end }}
      {{ with .Sitemap.ChangeFreq }}<changefreq>{{ . }}</changefreq>{{ end -}}
      {{ if ge .Sitemap.Priority 0.0 }}<priority>{{ .Sitemap.Priority }}</priority>{{ end -}}
      {{- if .IsTranslated -}}
      {{ range .AllTranslations -}}
      <xhtml:link rel="alternate" hreflang="{{ .Language.Params.languagecodehtml }}" href="{{ .Permalink }}" />
      {{ end -}}
      {{ range first 1 .AllTranslations }}<xhtml:link rel="alternate" hreflang="x-default" href="{{ $escapedurl }}" />{{ end }}
      {{ end -}}
      {{ with .Params.images -}}
      <image:image>
        <image:loc>{{ index . 0 | absURL }}</image:loc>
      </image:image>
      {{- end }}
    </url>
    {{- end -}}
  {{ end }}
</urlset>
